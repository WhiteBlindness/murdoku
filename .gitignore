# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Agent/AI tooling config that gets dropped into the project directory.
# These belong to the local environment, not to the game. A `git add -A` once
# swept 95 of these files (10k+ lines) into a commit on this PUBLIC repo.
.agents/
.codex/
.claude/
AGENTS.md
assets-dropbox/
